# Agenda
Scopo: separare MODEL / LAYOUT / RENDER / LISTENERS.

- model.js: normalizzazione appuntamenti (data/ora/oraFine/durata)
- layout.js: collision detection + calcolo colonne (width/left)
- render.js: disegno blocchi e posizionamento nella griglia
- listeners.js: drag 15', click slot, filtri, dialog
