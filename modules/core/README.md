# Core
Qui finiranno utilities e store condivisi.

- store.js: lettura/scrittura localStorage + chiavi
- time.js: parsing/format/normalizzazioni date/ore (no UTC shift)
- dom.js: helper DOM (qs, on, delegate, ecc.)
